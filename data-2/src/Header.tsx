import styled from 'styled-components'

const StyledHeader = styled.header`
  background-color: #2B3950;
  color: white;
  text-align: left;
  padding: 0.8rem;
`;

export default function Header(){
  return <StyledHeader>Dashboard / Deffects</StyledHeader>
};
